# Runtime dependencies (imported by src/ and scripts/)
numpy
pandas
scipy
matplotlib
nistrng

# Development tools (used via CLI, not imported)
ruff
pytest

# For an exact, reproducible install use the pinned versions in requirements.lock.txt
